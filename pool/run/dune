(executable
 (name run)
 (preprocess
  (pps lwt_ppx ppx_deriving.show))
 (libraries
  caqti-driver-mariadb
  command
  database
  guard
  queue
  routes
  service
  sihl
  system_event
  tenant
  text_message))
