(library
 (name admin)
 (libraries
  ocauth
  pool_common
  pool_tenant
  pool_user
  ptime
  role
  service
  sihl
  sihl-user
  utils)
 (preprocess
  (pps lwt_ppx ppx_deriving.eq ppx_deriving.show)))

(include_subdirs unqualified)
